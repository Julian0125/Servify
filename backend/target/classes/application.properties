# Server Configuration
server.port=8080

# Application Name
spring.application.name=Servify API

# CORS Configuration (for React frontend)
cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Logging
logging.level.com.servify=DEBUG
